{
  "name": "sparkless",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/replace-urls.js",
    "build:vercel": "vite build && VITE_BACKEND_URL=$BACKEND_URL node scripts/replace-urls.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@google/genai": "^1.34.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
